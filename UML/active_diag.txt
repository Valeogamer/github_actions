@startuml
|Start|
if (Получение текстовой информации) then (Да)
  :Получение информации;
else (Нет)
  :Завершение работы;
  stop
endif
|Шаг 1| 
if (Подготовка текстовых данных) then (Да)
  :Обработка текстов;
else (Нет)
  :Завершение работы;
  stop
endif
|Шаг 2|
if (Обучение модели) then (Да)
  :Обучение модели;
else (Нет)
  :Завершение работы;
  stop
endif
|Шаг 3|
if (Определение фейковых новостей) then (Да)
  :Отображение результатов;
else (Нет)
  :Завершение работы;
  stop
endif
|End|
@enduml
